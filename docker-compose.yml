version: '3.8'

services:
  dev:
    build:
      context: ./docker/dev
    volumes:
      - "stack:/root/.stack"
    
  db:
    image: postgres
    restart: unless-stopped
    environment:
      POSTGRES_PASSWORD: example
    volumes:
      - "db-data:/var/lib/postgresql/data"

  adminer:
    image: adminer
    restart: unless-stopped
    ports:
      - 8080:8080


volumes:
  db-data:
  stack:
